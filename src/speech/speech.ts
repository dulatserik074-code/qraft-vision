import type{Priority}from'../types';export class SpeechService{enabled=true;rate=1;language='ru-RU';last='';speak(text:string,priority:Priority='medium'){this.last=text;if(!this.enabled||!('speechSynthesis'in window))return;if(priority==='high')speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=this.language;u.rate=this.rate;speechSynthesis.speak(u)}repeat(){if(this.last)this.speak(this.last)}stop(){speechSynthesis?.cancel()}}
