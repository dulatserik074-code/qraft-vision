import type{Priority}from'../types';export class SpeechService{enabled=true;rate=1;language='ru-RU';voiceURI='';last='';getVoices(){return'speechSynthesis'in window?speechSynthesis.getVoices():[]}speak(text:string,priority:Priority='medium'){this.last=text;if(!this.enabled||!('speechSynthesis'in window))return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=this.language;u.rate=this.rate;const voices=this.getVoices(),selected=voices.find(v=>v.voiceURI===this.voiceURI)||voices.find(v=>v.lang.toLowerCase().startsWith('ru'));if(selected)u.voice=selected;speechSynthesis.speak(u)}repeat(){if(this.last)this.speak(this.last)}stop(){if('speechSynthesis'in window)speechSynthesis.cancel()}}
