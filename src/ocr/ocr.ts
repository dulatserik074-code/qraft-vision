export async function recognizeRussian(source:HTMLCanvasElement,onProgress?:(value:number)=>void){const{createWorker}=await import('tesseract.js');const worker=await createWorker('rus',1,{logger:m=>{if(m.status==='recognizing text')onProgress?.(m.progress)}});try{const result=await worker.recognize(source);return result.data.text.replace(/\s+/g,' ').trim()}finally{await worker.terminate()}}
