export class CameraController{private stream?:MediaStream;constructor(private video:HTMLVideoElement,private canvas:HTMLCanvasElement){}async start(){if(!navigator.mediaDevices?.getUserMedia)throw new Error('Камера не поддерживается браузером');this.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:720}},audio:false});this.video.srcObject=this.stream;await this.video.play()}capture(){const w=this.video.videoWidth,h=this.video.videoHeight;if(!w||!h)throw new Error('Камера ещё не готова');this.canvas.width=w;this.canvas.height=h;this.canvas.getContext('2d')!.drawImage(this.video,0,0,w,h);return this.canvas}stop(){this.stream?.getTracks().forEach(t=>t.stop());this.stream=undefined;this.video.srcObject=null}get active(){return!!this.stream}}
